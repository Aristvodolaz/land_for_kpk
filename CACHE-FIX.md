# 🔧 РЕШЕНИЕ ПРОБЛЕМЫ С КЭШЕМ CSS

## ❌ ПРОБЛЕМА:
Стили не обновляются на сервере, но локально всё работает.

## ✅ РЕШЕНИЕ:

### Быстрое решение (1 минута):

1. **Очистите кэш браузера:**
   - **Windows:** `Ctrl + Shift + R`
   - **Mac:** `Cmd + Shift + R`
   - **Chrome:** F12 → правый клик на кнопке Reload → "Empty Cache and Hard Reload"

2. **Проверьте результат**

Если не помогло → см. ниже

---

## 🔍 ЧТО Я УЖЕ СДЕЛАЛ:

### ✅ Добавил версионирование CSS

**Все HTML-файлы теперь загружают:**
```html
<link rel="stylesheet" href="css/style.css?v=3.0">
```

**Это заставляет браузер загрузить новый файл!**

При следующем обновлении просто измените версию:
- `?v=3.1`
- `?v=3.2`
- `?v=4.0` (при большом обновлении)

---

## 📋 ПОШАГОВАЯ ДИАГНОСТИКА:

### Шаг 1: Проверьте что файлы загружены на сервер

**Подключитесь к серверу:**
```bash
ssh username@your-server.com
```

**Проверьте файлы:**
```bash
# Проверьте дату изменения
ls -lh /var/www/landing/css/style.css

# Проверьте размер (должен быть ~80 КБ)
du -h /var/www/landing/css/style.css

# Посмотрите последние строки
tail -100 /var/www/landing/css/style.css | grep "telegram-btn"
```

**Должно быть найдено:**
- Стили `.telegram-btn`
- Стили `.course-card`
- Стили `.module-detailed`

**Если нет → загрузите файл заново:**
```bash
scp d:/landing/css/style.css username@server:/var/www/landing/css/
```

---

### Шаг 2: Очистите кэш на сервере

**Я создал скрипт `clear-cache.sh`**

**Использование:**
```bash
# На сервере
cd /var/www/landing
chmod +x clear-cache.sh
./clear-cache.sh
```

**Скрипт автоматически:**
- Очистит кэш Nginx/Apache
- Обновит временные метки файлов
- Перезагрузит веб-сервер

---

### Шаг 3: Проверьте что CSS действительно загружается

**В браузере:**

1. Откройте сайт
2. Нажмите **F12** (открыть DevTools)
3. Вкладка **Network**
4. **Обновите страницу (F5)**
5. Найдите `style.css?v=3.0`
6. Кликните на него
7. Вкладка **Response** → проверьте что там новые стили

**Должны быть видны:**
```css
.telegram-btn {
    width: 80px;
    height: 80px;
    ...
}
```

**Если нет → кэш не очищен, см. Шаг 4**

---

### Шаг 4: Ядерный вариант (100% работает)

**Измените имя файла CSS:**

1. **Переименуйте файл на сервере:**
```bash
mv /var/www/landing/css/style.css /var/www/landing/css/style-v3.css
```

2. **Обновите все HTML-файлы:**
```html
<!-- Было: -->
<link rel="stylesheet" href="css/style.css?v=3.0">

<!-- Стало: -->
<link rel="stylesheet" href="css/style-v3.css">
```

3. **Загрузите обновлённые HTML на сервер**

**Это 100% обойдёт любой кэш!**

---

## 🌐 ЕСЛИ ИСПОЛЬЗУЕТЕ CDN/CLOUDFLARE:

### Очистка кэша Cloudflare:

**Вариант 1: Через панель управления**
1. Войдите в Cloudflare
2. Выберите ваш домен
3. **Caching** → **Purge Cache** → **Purge Everything**

**Вариант 2: Через API**

Раскомментируйте секцию в `clear-cache.sh` и настройте:

```bash
ZONE_ID="your_zone_id"
API_TOKEN="your_api_token"

curl -X POST "https://api.cloudflare.com/client/v4/zones/$ZONE_ID/purge_cache" \
     -H "Authorization: Bearer $API_TOKEN" \
     -H "Content-Type: application/json" \
     --data '{"purge_everything":true}'
```

---

## 📱 БЫСТРАЯ ПРОВЕРКА:

### Вы поймёте что всё работает когда:

✅ Кнопка Telegram большая (80×80px на desktop)
✅ Кнопка появляется с анимацией через 1 секунду
✅ Кнопка пульсирует
✅ При hover увеличивается
✅ Выпадающее меню "Курсы" работает
✅ Карточки курсов на главной странице (4 штуки)
✅ Breadcrumbs на страницах предметов

---

## 🔄 АВТОМАТИЗАЦИЯ НА БУДУЩЕЕ:

### При каждом обновлении CSS:

1. **Измените версию в HTML:**
   ```html
   <link rel="stylesheet" href="css/style.css?v=3.1">
   ```

2. **Загрузите файлы:**
   ```bash
   ./deploy.sh
   ```

3. **Очистите кэш:**
   ```bash
   ssh user@server "./clear-cache.sh"
   ```

4. **Обновите браузер:**
   ```
   Ctrl + Shift + R
   ```

---

## ✅ ИТОГО:

### Проблема решена через:

1. ✅ **Версионирование CSS** (`?v=3.0`)
2. ✅ **Скрипт очистки кэша** (`clear-cache.sh`)
3. ✅ **Увеличена кнопка Telegram** (80×80px)

### Кнопка Telegram:
- **Размер:** 80×80 пикселей (desktop), 68×68px (mobile)
- **Позиция:** Правый нижний угол, всегда видна
- **Анимация:** Появление + пульсация
- **Ссылка:** Замените `https://t.me/your_channel` на вашу

---

**Теперь всё должно работать! 🎉**

**Если проблема осталась:**
- Напишите мне, я помогу с диагностикой
- Приложите скриншот из DevTools → Network → style.css

---

**Версия:** 3.0.0  
**Дата:** 26 января 2026

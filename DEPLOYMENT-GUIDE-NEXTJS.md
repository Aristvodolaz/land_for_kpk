# 🚀 Полная инструкция по деплою Next.js на свой сервер

## 📋 Содержание

1. [Предварительные требования](#предварительные-требования)
2. [Локальная подготовка](#локальная-подготовка)
3. [Настройка сервера](#настройка-сервера)
4. [Деплой проекта](#деплой-проекта)
5. [Настройка HTTPS](#настройка-https)
6. [Автоматический деплой](#автоматический-деплой)
7. [Решение проблем](#решение-проблем)

---

## Предварительные требования

### На локальном компьютере:

- ✅ Node.js 18+ и npm
- ✅ Git
- ✅ SSH клиент

### На сервере:

- ✅ Ubuntu / Debian (или другой Linux)
- ✅ Nginx
- ✅ SSH доступ (желательно по ключу)
- ✅ Домен, направленный на IP сервера

---

## Локальная подготовка

### 1. Установите зависимости

```bash
cd d:/landing
npm install
```

### 2. Проверьте что проект собирается

```bash
npm run build
```

Должна создаться папка `/out` со статическими файлами.

### 3. Проверьте локально

```bash
npm run dev
```

Откройте http://localhost:3000 и убедитесь что всё работает.

---

## Настройка сервера

### 1. Подключитесь к серверу

```bash
ssh username@your-server.com
```

### 2. Обновите систему

```bash
sudo apt update && sudo apt upgrade -y
```

### 3. Установите Nginx (если не установлен)

```bash
sudo apt install nginx -y
sudo systemctl start nginx
sudo systemctl enable nginx
```

Проверьте:

```bash
sudo systemctl status nginx
```

### 4. Создайте директорию для сайта

```bash
sudo mkdir -p /var/www/landing
sudo chown -R $USER:$USER /var/www/landing
```

### 5. Настройте Nginx

Скопируйте конфиг на сервер:

```bash
# На локальном компьютере
scp nginx-nextjs.conf username@your-server.com:/tmp/landing.conf
```

На сервере:

```bash
sudo mv /tmp/landing.conf /etc/nginx/sites-available/landing.conf

# Отредактируйте конфиг (замените your-domain.com)
sudo nano /etc/nginx/sites-available/landing.conf

# Создайте символическую ссылку
sudo ln -s /etc/nginx/sites-available/landing.conf /etc/nginx/sites-enabled/

# Удалите дефолтный конфиг (опционально)
sudo rm /etc/nginx/sites-enabled/default

# Проверьте конфигурацию
sudo nginx -t

# Перезагрузите Nginx
sudo systemctl reload nginx
```

---

## Деплой проекта

### Вариант 1: Автоматический деплой (рекомендуется)

#### 1. Настройте скрипт деплоя

Откройте `deploy-nextjs.sh` и замените:

```bash
SERVER_USER="username"          # Ваш SSH пользователь
SERVER_HOST="your-server.com"   # Ваш домен или IP
SERVER_PATH="/var/www/landing"  # Путь на сервере
```

#### 2. Сделайте скрипт исполняемым

```bash
chmod +x deploy-nextjs.sh
```

#### 3. Запустите деплой

```bash
./deploy-nextjs.sh
```

Скрипт автоматически:
- Проверит соединение с сервером
- Соберёт проект
- Создаст бэкап на сервере
- Загрузит файлы
- Перезагрузит Nginx

---

### Вариант 2: Ручной деплой

#### 1. Соберите проект локально

```bash
npm run build
```

#### 2. Загрузите на сервер через rsync

```bash
rsync -avz --delete out/ username@your-server.com:/var/www/landing/
```

Или через FTP/SFTP (FileZilla, WinSCP):

- Подключитесь к серверу
- Загрузите содержимое папки `/out` в `/var/www/landing/`

#### 3. Установите права

```bash
ssh username@your-server.com
cd /var/www/landing
chmod -R 755 .
```

#### 4. Перезагрузите Nginx

```bash
sudo nginx -t
sudo systemctl reload nginx
```

---

## Настройка HTTPS

### 1. Установите Certbot

```bash
sudo apt install certbot python3-certbot-nginx -y
```

### 2. Получите SSL сертификат

```bash
sudo certbot --nginx -d your-domain.com -d www.your-domain.com
```

Certbot автоматически:
- Получит сертификат от Let's Encrypt
- Настроит Nginx для HTTPS
- Создаст редирект с HTTP на HTTPS

### 3. Проверьте автопродление

```bash
sudo certbot renew --dry-run
```

Сертификат будет автоматически обновляться каждые 90 дней.

---

## Автоматический деплой

### Настройка SSH ключей (без пароля)

#### 1. Создайте SSH ключ (если нет)

```bash
ssh-keygen -t rsa -b 4096 -C "your_email@example.com"
```

#### 2. Скопируйте ключ на сервер

```bash
ssh-copy-id username@your-server.com
```

#### 3. Проверьте подключение без пароля

```bash
ssh username@your-server.com
```

Теперь скрипт `deploy-nextjs.sh` будет работать автоматически без запроса пароля!

---

### Деплой по расписанию (опционально)

Если вы хотите автоматически деплоить при каждом коммите, используйте:

**GitHub Actions** (если используете GitHub):

Создайте `.github/workflows/deploy.yml`:

```yaml
name: Deploy to Server

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
      
      - name: Install dependencies
        run: npm install
      
      - name: Build project
        run: npm run build
      
      - name: Deploy to server
        uses: appleboy/scp-action@master
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          source: "out/"
          target: "/var/www/landing/"
```

Добавьте секреты в GitHub:
- `SERVER_HOST`
- `SERVER_USER`
- `SSH_PRIVATE_KEY`

---

## Решение проблем

### Проблема 1: Страницы возвращают 404

**Причина:** Неправильная конфигурация Nginx

**Решение:**

Убедитесь что в `nginx.conf` есть:

```nginx
location / {
    try_files $uri $uri.html $uri/ =404;
}
```

Перезагрузите Nginx:

```bash
sudo nginx -t
sudo systemctl reload nginx
```

---

### Проблема 2: CSS не загружается

**Причина:** Неправильные права доступа или путь

**Решение:**

```bash
# Проверьте структуру файлов
ssh username@your-server.com
cd /var/www/landing
ls -la

# Должны быть: index.html, _next/, images/ и т.д.

# Установите права
chmod -R 755 /var/www/landing
```

Проверьте в браузере DevTools (F12) → Network:
- Файлы должны загружаться со статусом 200
- Если 404 — неправильный путь

---

### Проблема 3: "Permission denied" при деплое

**Причина:** Нет прав на запись в `/var/www/landing`

**Решение:**

```bash
# На сервере
sudo chown -R $USER:$USER /var/www/landing
chmod -R 755 /var/www/landing
```

---

### Проблема 4: Старые стили/кэш

**Причина:** Браузер кэширует старые файлы

**Решение:**

1. **В браузере:**
   - `Ctrl + Shift + R` (Windows)
   - `Cmd + Shift + R` (Mac)

2. **На сервере:**
   ```bash
   sudo systemctl reload nginx
   ```

3. **Изменить версию CSS** (если кэширование агрессивное):
   
   В Nginx конфиге измените:
   ```nginx
   location ~* \.(css|js)$ {
       expires 1h; # Вместо 1y
   }
   ```

---

### Проблема 5: Nginx не запускается

**Причина:** Ошибка в конфигурации

**Решение:**

```bash
# Проверьте синтаксис
sudo nginx -t

# Посмотрите логи ошибок
sudo tail -50 /var/log/nginx/error.log

# Исправьте ошибки в конфиге
sudo nano /etc/nginx/sites-available/landing.conf

# Попробуйте снова
sudo nginx -t
sudo systemctl reload nginx
```

---

## 📊 Проверка после деплоя

### Чеклист:

- [ ] Сайт открывается: `http://your-domain.com`
- [ ] Все страницы работают (главная, физика, химия, биология, математика)
- [ ] CSS загружается корректно
- [ ] Изображения отображаются
- [ ] Telegram кнопка работает и ведёт на правильный канал
- [ ] Навигация работает (Header, Footer, ссылки)
- [ ] Мобильная версия корректна (откройте DevTools → Toggle device toolbar)
- [ ] HTTPS работает (если настроили)
- [ ] Редирект www → без www работает (если настроили)

---

## 🔄 Обновление сайта

После внесения изменений в код:

```bash
# 1. Локально соберите проект
npm run build

# 2. Запустите деплой
./deploy-nextjs.sh

# 3. Очистите кэш браузера
# Ctrl + Shift + R
```

---

## 🎯 Быстрая команда деплоя (одна строка)

```bash
npm run build && rsync -avz --delete out/ username@server:/var/www/landing/ && ssh username@server "sudo systemctl reload nginx"
```

Добавьте в `package.json`:

```json
{
  "scripts": {
    "deploy": "npm run build && rsync -avz --delete out/ username@server:/var/www/landing/"
  }
}
```

Использование:

```bash
npm run deploy
```

---

## 📞 Помощь

Если что-то не работает:

1. Проверьте логи Nginx: `sudo tail -100 /var/log/nginx/error.log`
2. Проверьте права: `ls -la /var/www/landing`
3. Проверьте DNS: `nslookup your-domain.com`
4. Проверьте firewall: `sudo ufw status`

---

**Версия:** 1.0.0  
**Дата:** 6 февраля 2026

# ✅ ОТЧЁТ: Миграция с HTML на Next.js

## 📋 ЗАДАЧА:

Переделать существующий лендинг (HTML/CSS/JS) на Next.js с деплоем на свой сервер.

---

## ✨ ЧТО ВЫПОЛНЕНО:

### 1. ✅ Инициализация Next.js проекта

**Создано:**
- `package.json` — зависимости и скрипты
- `next.config.js` — конфигурация с `output: 'export'` для статического экспорта
- `tsconfig.json` — настройки TypeScript
- `.gitignore` — файлы для игнорирования

**Результат:** Проект готов к разработке и сборке

---

### 2. ✅ Структура данных

**Создано:**
- `src/data/courses.ts` — TypeScript файл с данными о курсах

**Содержит:**
- Интерфейсы: `Speaker`, `Module`, `Course`
- Полные данные курса физики (3 модуля, спикеры)
- Готовая структура для добавления остальных предметов

**Преимущества:**
- Вся информация в одном месте
- Легко редактировать и добавлять новые курсы
- Типизация TypeScript предотвращает ошибки

---

### 3. ✅ Компоненты

#### Header (`src/components/Header/`)
- Фиксированный header с логотипом
- Выпадающее меню "Курсы" с 4 предметами
- Бургер-меню для мобильных
- Эффект тени при скролле
- CSS Modules для изоляции стилей

#### Footer (`src/components/Footer/`)
- 3 колонки: О курсах, Курсы, Контакты
- Адаптивная сетка
- Ссылки на все разделы

#### TelegramButton (`src/components/TelegramButton/`)
- Плавающая кнопка (80×80px desktop, 68×68px mobile)
- Анимация появления через 1 секунду
- Пульсация для привлечения внимания
- Hover-эффект

**Все компоненты:**
- Переиспользуемые
- Типизированные (TypeScript)
- Адаптивные (responsive)
- С изолированными стилями (CSS Modules)

---

### 4. ✅ Страницы

#### Главная страница (`src/app/page.tsx`)

**Блоки:**
- Hero (заголовок + CTA)
- Почему удобен (4 карточки)
- Практическая польза (4 блока)
- Курсы по предметам (4 карточки с ссылками)
- Формат обучения (4 пункта)
- Отзывы (3 отзыва)

**Особенности:**
- Все данные структурированы
- CSS Modules для каждого блока
- Hover-эффекты на карточках
- Адаптивная сетка

#### Страница курса физики (`src/app/physics/page.tsx`)

**Блоки:**
- Hero с breadcrumbs и мета-информацией
- Программа курсов (3 модуля):
  - Содержание модуля (список тем)
  - Практические результаты (сетка с иконками)
  - Для кого предназначен
  - CTA блок "Записаться на модуль"
- Спикеры курсов (группированы по модулям):
  - Методист
  - Приглашённые лекторы
- Финальный CTA

**Особенности:**
- Данные берутся из `src/data/courses.ts`
- Динамическая генерация модулей через `.map()`
- Карточки спикеров с фото
- SEO-friendly (metadata, breadcrumbs)

#### Страницы остальных предметов

**Созданы заглушки:**
- `src/app/chemistry/page.tsx`
- `src/app/biology/page.tsx`
- `src/app/mathematics/page.tsx`

**Структура готова**, осталось добавить данные в `courses.ts`

---

### 5. ✅ Стили

#### Глобальные стили (`src/styles/globals.css`)

**Включает:**
- CSS переменные для цветов и отступов
- Базовые стили (reset, body, container)
- Стили кнопок (.btn, .btn--primary)
- Анимации (fadeIn, slideInFromBottom, spin)
- Focus states для доступности

#### Модульные стили

**Созданы CSS Modules для:**
- `page.module.css` — главная страница
- `physics.module.css` — страница курса (переиспользуется остальными)
- `Header.module.css` — хедер
- `Footer.module.css` — футер
- `TelegramButton.module.css` — Telegram кнопка

**Преимущества:**
- Нет конфликтов стилей
- Автоматическое именование классов
- Легко поддерживать

---

### 6. ✅ Деплой на свой сервер

#### Конфигурация Next.js для статического экспорта

```javascript
// next.config.js
{
  output: 'export',      // Статический HTML
  images: {
    unoptimized: true,   // Для статического экспорта
  },
  trailingSlash: true,   // Для совместимости с Nginx
}
```

#### Nginx конфигурация (`nginx-nextjs.conf`)

**Включает:**
- Правила обработки статических страниц (`try_files $uri $uri.html`)
- Кэширование (1 год для статики, 1 час для HTML)
- GZIP сжатие
- Security headers (X-Frame-Options, X-Content-Type-Options и т.д.)
- Готовые блоки для HTTPS
- Кастомные страницы ошибок

#### Скрипт автодеплоя (`deploy-nextjs.sh`)

**Автоматизирует:**
1. Проверку соединения с сервером
2. Установку зависимостей (если нужно)
3. Сборку проекта (`npm run build`)
4. Создание бэкапа на сервере
5. Загрузку файлов через rsync
6. Установку прав доступа
7. Перезагрузку Nginx

**Использование:**

```bash
./deploy-nextjs.sh
```

#### Документация

**Созданы подробные инструкции:**
- `README-NEXTJS.md` — обзор проекта и быстрый старт
- `DEPLOYMENT-GUIDE-NEXTJS.md` — полная инструкция по деплою:
  - Настройка сервера
  - Настройка Nginx
  - Получение SSL сертификата (Let's Encrypt)
  - Автоматический и ручной деплой
  - Решение типовых проблем

---

## 🔄 ЧТО ИЗМЕНИЛОСЬ:

### Было (HTML/CSS/JS):

```
landing/
├── index.html (432 строки)
├── physics.html
├── chemistry.html
├── biology.html
├── mathematics.html
├── css/style.css (1925 строк)
├── js/main.js
└── images/
```

**Проблемы:**
- Дублирование кода (header/footer в каждом файле)
- Сложно обновлять контент (захардкожен в HTML)
- Ручное управление всеми файлами
- Нет типизации

---

### Стало (Next.js):

```
landing/
├── src/
│   ├── app/                    # Страницы (React)
│   │   ├── layout.tsx          # Общий layout (header + footer)
│   │   ├── page.tsx            # Главная
│   │   └── physics/page.tsx    # Курсы (динамические)
│   ├── components/             # Компоненты
│   ├── data/courses.ts         # Данные в JSON-like формате
│   └── styles/                 # Модульные стили
├── public/                     # Статика
├── out/                        # Собранный сайт (HTML)
└── package.json
```

**Преимущества:**
- ✅ Один layout для всех страниц
- ✅ Данные отделены от разметки
- ✅ Компоненты переиспользуются
- ✅ TypeScript предотвращает ошибки
- ✅ Модульные стили (нет конфликтов)
- ✅ Автоматическая оптимизация
- ✅ Легко добавлять новые курсы

---

## 📊 СРАВНЕНИЕ:

| Параметр | HTML | Next.js |
|----------|------|---------|
| **Размер кода** | ~3000 строк | ~2000 строк (более структурировано) |
| **Дублирование** | Высокое (header/footer в каждом файле) | Нет (layout один раз) |
| **Обновление контента** | Ручное в каждом HTML | Только в `courses.ts` |
| **Типизация** | Нет | TypeScript |
| **Стили** | Один большой CSS файл | Модульные CSS (изолированные) |
| **SEO** | Ручные meta-теги | Автоматические (metadata) |
| **Деплой** | Ручная загрузка через FTP | Автоматический скрипт |
| **Скорость разработки** | Медленная (много файлов) | Быстрая (компоненты) |

---

## 🎯 КАК ИСПОЛЬЗОВАТЬ:

### 1. Разработка

```bash
# Установка
npm install

# Запуск локально
npm run dev

# Откройте http://localhost:3000
```

### 2. Добавление данных

**Для остальных предметов:**

1. Откройте `src/data/courses.ts`
2. Скопируйте структуру `physicsCourse`
3. Замените данные (название, модули, спикеры)
4. Экспортируйте в массив `courses`

**Пример:**

```typescript
export const chemistryCourse: Course = {
  id: 'chemistry',
  title: 'Курсы для учителей химии',
  // ... остальные поля
}

export const courses: Course[] = [
  physicsCourse,
  chemistryCourse, // Добавили
]
```

### 3. Сборка и деплой

```bash
# Автоматический деплой (всё за раз)
./deploy-nextjs.sh

# Или вручную:
npm run build                    # Создаст /out
rsync -avz out/ user@server:/www # Загрузит на сервер
```

---

## 📝 TODO (Что нужно сделать):

### Обязательно:

- [ ] Добавить данные курсов: Химия, Биология, Математика в `src/data/courses.ts`
- [ ] Заменить placeholder на реальные фото спикеров (30+ фото в `public/images/speakers/`)
- [ ] Настроить скрипт деплоя (заменить `SERVER_USER`, `SERVER_HOST` в `deploy-nextjs.sh`)
- [ ] Заменить ссылку на Telegram канал в `src/components/TelegramButton/TelegramButton.tsx`

### Опционально:

- [ ] Добавить форму обратной связи (API Route в Next.js или внешний сервис)
- [ ] Настроить аналитику (Google Analytics / Яндекс.Метрика)
- [ ] Добавить модальное окно для формы записи
- [ ] Получить SSL сертификат (Let's Encrypt)
- [ ] Настроить автодеплой через GitHub Actions

---

## 🚀 ДЕПЛОЙ НА СЕРВЕРЕ:

### Быстрый старт:

```bash
# 1. На локальном компьютере
cd d:/landing
npm install
npm run build

# 2. Загрузите на сервер
./deploy-nextjs.sh

# 3. На сервере настройте Nginx
scp nginx-nextjs.conf user@server:/tmp/
ssh user@server
sudo mv /tmp/nginx-nextjs.conf /etc/nginx/sites-available/landing.conf
sudo ln -s /etc/nginx/sites-available/landing.conf /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl reload nginx

# 4. Откройте в браузере
# http://your-domain.com
```

### Полная инструкция:

См. `DEPLOYMENT-GUIDE-NEXTJS.md`

---

## 🎨 КАСТОМИЗАЦИЯ:

### Изменить цвета:

```css
/* src/styles/globals.css */
:root {
  --color-bg: #F3F0EB;       /* Фон */
  --color-primary: #FF632C;  /* Основной акцент */
}
```

### Изменить тексты главной страницы:

Откройте `src/app/page.tsx` и отредактируйте JSX.

### Изменить данные курсов:

Откройте `src/data/courses.ts` и обновите данные.

---

## ⚡ ПРЕИМУЩЕСТВА NEXT.JS:

1. **Компонентный подход** — Header/Footer один раз, используется везде
2. **Данные отдельно** — контент в `courses.ts`, легко редактировать
3. **TypeScript** — ловит ошибки до запуска
4. **CSS Modules** — стили не конфликтуют между компонентами
5. **Статический экспорт** — работает на любом сервере (Nginx, Apache, Netlify)
6. **SEO-friendly** — автоматические meta-теги, sitemap
7. **Автодеплой** — один скрипт заменяет ручную работу

---

## 📞 ПОДДЕРЖКА:

Если возникли проблемы:

1. Проверьте логи сборки: `npm run build`
2. Проверьте логи Nginx: `sudo tail -100 /var/log/nginx/error.log`
3. Убедитесь что `/out` существует после сборки
4. Проверьте права доступа: `ls -la /var/www/landing`

---

## ✅ ИТОГО:

### Миграция выполнена успешно! 🎉

**Создано:**
- ✅ Полноценный Next.js проект с TypeScript
- ✅ 5 страниц (1 главная + 4 предмета)
- ✅ 3 переиспользуемых компонента
- ✅ Структура данных для курсов
- ✅ Модульные стили
- ✅ Конфигурация Nginx
- ✅ Скрипт автодеплоя
- ✅ Подробная документация

**Осталось:**
- Добавить данные для 3 предметов (химия, биология, математика)
- Заменить placeholder фото
- Настроить деплой на вашем сервере

---

**Версия:** 1.0.0 (Next.js Migration)  
**Дата:** 6 февраля 2026  
**Автор:** AI Assistant

# 📦 Полное руководство по деплою лендинга

## ✅ ЧТО УЛУЧШЕНО В ПОСЛЕДНЕЙ ВЕРСИИ:

### 1. Статичный Header с навигацией
- ✅ Фиксированный header с логотипом
- ✅ Навигация по блокам: Преимущества, Программа, Спикеры, Отзывы
- ✅ Кнопка "Записаться" в header
- ✅ Адаптивное бургер-меню для мобильных
- ✅ Тень при прокрутке для лучшей видимости

### 2. Улучшенная форма обратной связи
- ✅ Валидация в реальном времени
- ✅ Визуальная обратная связь (зелёные/красные поля)
- ✅ Индикатор загрузки при отправке
- ✅ Красивое сообщение об успехе с анимацией
- ✅ Чекбокс согласия на обработку данных
- ✅ Placeholder'ы для всех полей

### 3. Обновлённая секция спикеров
- ✅ Структурирована по курсам: Физика, Химия, Биология, Математика
- ✅ Компактные карточки с фото и ролями (Методист/Приглашённый лектор)
- ✅ Все реальные преподаватели с полными данными
- ✅ Место для фото каждого спикера (placeholder.svg)

### 4. Файлы для деплоя
- ✅ `.htaccess` — для Apache
- ✅ `nginx.conf` — для Nginx
- ✅ `netlify.toml` — для Netlify
- ✅ `vercel.json` — для Vercel
- ✅ `deploy.sh` — скрипт автодеплоя на свой сервер

---

## 🚀 СПОСОБЫ ДЕПЛОЯ

### ⚡ СПОСОБ 1: Netlify (Самый простой, рекомендуется)

**Время деплоя:** 3 минуты  
**Сложность:** ⭐☆☆☆☆

#### Шаг 1: Drag & Drop

1. Откройте [netlify.com/drop](https://app.netlify.com/drop)
2. Перетащите папку `landing` в окно браузера
3. Дождитесь загрузки (~30 секунд)

✅ **Готово!** Netlify выдаст URL: `https://random-name-12345.netlify.app`

#### Шаг 2: Настройка (опционально)

1. Зарегистрируйтесь на Netlify
2. Перейдите в настройки сайта
3. **Site settings** → **Change site name** → введите `your-name-kpk`
4. Теперь URL: `https://your-name-kpk.netlify.app`

#### Шаг 3: Собственный домен (опционально)

1. **Domain settings** → **Add custom domain**
2. Введите свой домен: `kpk.yourdomain.com`
3. Следуйте инструкциям для настройки DNS

#### Файл конфигурации

Уже создан: `netlify.toml`

Проверьте и замените `yourdomain.com` на ваш домен:

```toml
[[redirects]]
  from = "https://www.yourdomain.com/*"
  to = "https://yourdomain.com/:splat"
  status = 301
  force = true
```

---

### ⚡ СПОСОБ 2: Vercel

**Время деплоя:** 5 минут  
**Сложность:** ⭐⭐☆☆☆

#### Через CLI:

```bash
# Установка Vercel CLI (один раз)
npm i -g vercel

# Переход в папку проекта
cd d:/landing

# Деплой
vercel

# Следуйте инструкциям в терминале
```

#### Через GitHub:

1. Создайте репозиторий на GitHub
2. Загрузите файлы проекта
3. Подключите репозиторий к Vercel
4. Vercel автоматически задеплоит

#### Файл конфигурации

Уже создан: `vercel.json`

---

### ⚡ СПОСОБ 3: GitHub Pages (Бесплатно)

**Время деплоя:** 10 минут  
**Сложность:** ⭐⭐☆☆☆

#### Шаг 1: Создание репозитория

```bash
cd d:/landing

# Инициализация Git
git init
git add .
git commit -m "Initial commit"

# Создайте репозиторий на github.com
# Затем:
git remote add origin https://github.com/username/landing-kpk.git
git branch -M main
git push -u origin main
```

#### Шаг 2: Настройка GitHub Pages

1. Откройте репозиторий на GitHub
2. **Settings** → **Pages**
3. **Source:** Deploy from a branch
4. **Branch:** main → **Folder:** / (root)
5. Нажмите **Save**

✅ Через 1-2 минуты сайт будет доступен: `https://username.github.io/landing-kpk/`

#### Собственный домен:

1. **Settings** → **Pages** → **Custom domain**
2. Введите: `kpk.yourdomain.com`
3. Добавьте CNAME-запись в DNS:

```
CNAME  kpk  username.github.io
```

---

### ⚡ СПОСОБ 4: Собственный сервер (VPS)

**Время деплоя:** 30 минут  
**Сложность:** ⭐⭐⭐⭐☆

#### Требования:

- VPS с Ubuntu/Debian
- SSH доступ
- Установленный Nginx или Apache

---

#### ВАРИАНТ A: Nginx (рекомендуется)

**Шаг 1: Подключение к серверу**

```bash
ssh username@your-server.com
```

**Шаг 2: Установка Nginx**

```bash
sudo apt update
sudo apt install nginx -y
```

**Шаг 3: Создание директории**

```bash
sudo mkdir -p /var/www/landing
sudo chown -R $USER:$USER /var/www/landing
```

**Шаг 4: Загрузка файлов**

Локально (с вашего компьютера):

```bash
# Используйте SCP или SFTP
scp -r d:/landing/* username@your-server.com:/var/www/landing/

# Или используйте готовый скрипт deploy.sh (см. ниже)
```

**Шаг 5: Настройка Nginx**

```bash
sudo nano /etc/nginx/sites-available/landing
```

Вставьте конфигурацию из файла `nginx.conf` (уже создан в проекте).

**Измените:**
- `server_name` на ваш домен
- `root` на `/var/www/landing`

**Шаг 6: Активация конфигурации**

```bash
sudo ln -s /etc/nginx/sites-available/landing /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl reload nginx
```

✅ **Готово!** Сайт доступен по вашему домену.

**Шаг 7: SSL-сертификат (Let's Encrypt)**

```bash
sudo apt install certbot python3-certbot-nginx -y
sudo certbot --nginx -d yourdomain.com -d www.yourdomain.com
```

Certbot автоматически настроит HTTPS.

---

#### ВАРИАНТ B: Apache

**Шаг 1-3:** Аналогично Nginx

**Шаг 4: Загрузка файлов**

```bash
scp -r d:/landing/* username@your-server.com:/var/www/landing/
```

**Шаг 5: Настройка Apache**

```bash
sudo nano /etc/apache2/sites-available/landing.conf
```

Вставьте:

```apache
<VirtualHost *:80>
    ServerName yourdomain.com
    ServerAlias www.yourdomain.com
    DocumentRoot /var/www/landing

    <Directory /var/www/landing>
        Options Indexes FollowSymLinks
        AllowOverride All
        Require all granted
    </Directory>

    ErrorLog ${APACHE_LOG_DIR}/landing_error.log
    CustomLog ${APACHE_LOG_DIR}/landing_access.log combined
</VirtualHost>
```

**Шаг 6: Активация**

```bash
sudo a2ensite landing.conf
sudo a2enmod rewrite
sudo systemctl reload apache2
```

**Шаг 7: SSL (Let's Encrypt)**

```bash
sudo apt install certbot python3-certbot-apache -y
sudo certbot --apache -d yourdomain.com -d www.yourdomain.com
```

---

#### АВТОМАТИЗАЦИЯ: Скрипт deploy.sh

Уже создан в проекте: `deploy.sh`

**Настройка:**

1. Откройте `deploy.sh`
2. Измените переменные:

```bash
SERVER_USER="your_username"
SERVER_HOST="your-server.com"
SERVER_PORT="22"
SERVER_PATH="/var/www/landing"
```

3. Сделайте исполняемым:

```bash
chmod +x deploy.sh
```

4. Запустите:

```bash
./deploy.sh
```

Скрипт:
- ✅ Создаст бэкап текущей версии
- ✅ Загрузит файлы через rsync
- ✅ Настроит права доступа
- ✅ Перезапустит веб-сервер

---

### ⚡ СПОСОБ 5: Хостинг (shared hosting)

**Время деплоя:** 15 минут  
**Сложность:** ⭐⭐☆☆☆

Подходит для обычных хостингов (Timeweb, Beget, и т.д.)

**Шаг 1: Подключение через FTP**

Используйте FileZilla или любой FTP-клиент:

- Host: `ftp.yourdomain.com`
- Username: `ваш логин`
- Password: `ваш пароль`
- Port: `21`

**Шаг 2: Загрузка файлов**

1. Перейдите в папку `public_html` (или `www`)
2. Загрузите все файлы из папки `d:/landing/`

**Шаг 3: Настройка .htaccess**

Файл `.htaccess` уже создан и будет загружен автоматически.

Проверьте настройки внутри и измените домен при необходимости.

✅ **Готово!** Сайт доступен по вашему домену.

---

## 🔧 НАСТРОЙКА ФОРМЫ ОБРАТНОЙ СВЯЗИ

После деплоя нужно подключить отправку формы.

### ВАРИАНТ 1: Formspree (Рекомендуется)

1. Зарегистрируйтесь на [formspree.io](https://formspree.io/)
2. Создайте новую форму
3. Скопируйте Form Endpoint
4. Откройте `js/main.js` на сервере
5. Найдите секцию `ВАРИАНТ 1: Formspree`
6. Раскомментируйте код и замените `ВАША_ФОРМА_ID`

### ВАРИАНТ 2: Свой backend (PHP)

Если у вас есть PHP на хостинге:

1. Создайте файл `send-mail.php` (пример в `FORM-INTEGRATION.md`)
2. Загрузите на сервер
3. В `js/main.js` раскомментируйте `ВАРИАНТ 2`
4. Измените email получателя в PHP

Подробнее см. `FORM-INTEGRATION.md`

---

## 📝 ФИНАЛЬНЫЙ ЧЕКЛИСТ

### Перед деплоем:

- [ ] Замените `https://ваш-домен.ru/` в `index.html` (Open Graph)
- [ ] Добавьте реальные фото спикеров (замените `placeholder.svg`)
- [ ] Обновите название в header (`КПК для педагогов`)
- [ ] Проверьте контактные данные
- [ ] Настройте форму обратной связи

### После деплоя:

- [ ] Проверьте сайт на разных устройствах
- [ ] Проверьте все ссылки и кнопки
- [ ] Отправьте тестовую форму
- [ ] Настройте аналитику (Google Analytics / Яндекс.Метрика)
- [ ] Проверьте Open Graph (через [Facebook Debugger](https://developers.facebook.com/tools/debug/))
- [ ] Запустите Lighthouse-аудит (DevTools → Lighthouse)

---

## 🎨 ДОБАВЛЕНИЕ ФОТО СПИКЕРОВ

### Подготовка фото:

1. **Размер:** 400x400px
2. **Формат:** JPG или PNG
3. **Вес:** до 100 КБ каждое (сожмите через [TinyPNG](https://tinypng.com/))

### Загрузка:

1. Сохраните фото с понятными именами:
   - `chernikova.jpg`
   - `chernikov.jpg`
   - и т.д.

2. Загрузите в папку `images/speakers/`

3. Замените в `index.html`:

```html
<!-- Было: -->
<img src="images/speakers/placeholder.svg" alt="...">

<!-- Стало: -->
<img src="images/speakers/chernikova.jpg" alt="...">
```

---

## 📊 АНАЛИТИКА

### Google Analytics:

Добавьте в `<head>` в `index.html`:

```html
<!-- Google Analytics -->
<script async src="https://www.googletagmanager.com/gtag/js?id=GA_TRACKING_ID"></script>
<script>
  window.dataLayer = window.dataLayer || [];
  function gtag(){dataLayer.push(arguments);}
  gtag('js', new Date());
  gtag('config', 'GA_TRACKING_ID');
</script>
```

### Яндекс.Метрика:

```html
<!-- Yandex.Metrika -->
<script type="text/javascript">
   (function(m,e,t,r,i,k,a){m[i]=m[i]||function(){(m[i].a=m[i].a||[]).push(arguments)};
   m[i].l=1*new Date();
   for (var j = 0; j < document.scripts.length; j++) {if (document.scripts[j].src === r) { return; }}
   k=e.createElement(t),a=e.getElementsByTagName(t)[0],k.async=1,k.src=r,a.parentNode.insertBefore(k,a)})
   (window, document, "script", "https://mc.yandex.ru/metrika/tag.js", "ym");

   ym(COUNTER_ID, "init", {
        clickmap:true,
        trackLinks:true,
        accurateTrackBounce:true
   });
</script>
```

---

## 🐛 РЕШЕНИЕ ПРОБЛЕМ

### Проблема: Header перекрывает контент

**Решение:** В `css/style.css` увеличьте `padding-top` у `body`.

### Проблема: Бургер-меню не работает

**Решение:** Убедитесь, что `js/main.js` загружается. Проверьте консоль (F12).

### Проблема: Форма не отправляется

**Решение:** Откройте консоль браузера, посмотрите ошибки. Скорее всего не настроен backend.

### Проблема: Изображения не грузятся

**Решение:** Проверьте пути. На сервере должна быть структура:
```
/var/www/landing/
├── images/
│   └── speakers/
│       └── placeholder.svg
```

---

## ✅ ИТОГО

Вы получили:

✅ **Лендинг с:**
- Статичным header с навигацией
- Улучшенной формой с валидацией
- Структурированной секцией спикеров (Физика, Химия, Биология, Математика)
- Адаптивным дизайном

✅ **Файлы для деплоя:**
- `.htaccess` (Apache)
- `nginx.conf` (Nginx)
- `netlify.toml` (Netlify)
- `vercel.json` (Vercel)
- `deploy.sh` (скрипт автодеплоя)

✅ **Документацию:**
- `README.md` — полная документация
- `QUICKSTART.md` — быстрый старт
- `UX-CONCEPT.md` — концепция дизайна
- `FORM-INTEGRATION.md` — интеграция формы
- `DEPLOY-GUIDE.md` — это руководство

---

**Рекомендуемый путь:**
1. Деплой на Netlify (3 минуты)
2. Настройка формы через Formspree (5 минут)
3. Добавление фото спикеров (10 минут)
4. Подключение аналитики (5 минут)

**Итого: ~25 минут до полностью рабочего лендинга!**

---

**Удачи с запуском! 🚀**

**Версия:** 2.0.0  
**Дата:** Январь 2026

# 🚀 КПК Landing — Next.js Version

## 📋 О проекте

Лендинг для онлайн-курсов повышения квалификации для педагогов.

**Технологии:**
- Next.js 14 (App Router)
- TypeScript
- CSS Modules
- Static Export (для деплоя на свой сервер)

---

## 🛠️ Установка и запуск

### 1. Установка зависимостей

```bash
npm install
```

### 2. Запуск в режиме разработки

```bash
npm run dev
```

Откройте [http://localhost:3010](http://localhost:3010) в браузере.

### 3. Сборка для продакшн

```bash
npm run build
```

Это создаст статические файлы в папке `/out`.

---

## 📁 Структура проекта

```
landing/
├── src/
│   ├── app/                    # Страницы (Next.js App Router)
│   │   ├── layout.tsx          # Основной layout
│   │   ├── page.tsx            # Главная страница
│   │   ├── physics/            # Страница курса физики
│   │   ├── chemistry/          # Страница курса химии
│   │   ├── biology/            # Страница курса биологии
│   │   └── mathematics/        # Страница курса математики
│   │
│   ├── components/             # React компоненты
│   │   ├── Header/             # Хедер с навигацией
│   │   ├── Footer/             # Футер
│   │   └── TelegramButton/     # Плавающая кнопка Telegram
│   │
│   ├── data/                   # Данные курсов
│   │   └── courses.ts          # Информация о курсах и спикерах
│   │
│   └── styles/                 # Глобальные стили
│       └── globals.css         # CSS переменные и базовые стили
│
├── public/                     # Статические файлы
│   ├── images/
│   └── favicon.svg
│
├── package.json
├── next.config.js              # Конфигурация Next.js
└── tsconfig.json               # Конфигурация TypeScript
```

---

## 🎨 Особенности

### ✅ Что уже сделано:

1. **5 страниц:**
   - Главная (обзор всех курсов)
   - Физика (детальная программа)
   - Химия, Биология, Математика (заглушки)

2. **Компоненты:**
   - Header с выпадающим меню
   - Footer с контактами
   - Telegram Button (плавающая кнопка)

3. **Данные:**
   - Вся информация о курсах в `src/data/courses.ts`
   - Легко редактировать и добавлять новые курсы

4. **Оптимизация:**
   - Static Export (быстрая загрузка)
   - CSS Modules (изолированные стили)
   - TypeScript (надежность кода)

---

## 🔧 Кастомизация

### Изменить цвета

Откройте `src/styles/globals.css` и измените CSS переменные:

```css
:root {
  --color-bg: #F3F0EB;          /* Фон */
  --color-text: #21201F;        /* Текст */
  --color-primary: #FF632C;     /* Основной акцент */
  --color-secondary: #3FA9F5;   /* Вторичный акцент */
}
```

### Изменить ссылку на Telegram

Откройте `src/components/TelegramButton/TelegramButton.tsx`:

```tsx
<a href="https://t.me/your_channel" ...>
```

Замените на:

```tsx
<a href="https://t.me/название_вашего_канала" ...>
```

### Добавить данные для остальных курсов

Откройте `src/data/courses.ts` и добавьте информацию по аналогии с `physicsCourse`.

---

## 📦 Деплой на свой сервер

### Шаг 1: Соберите проект

```bash
npm run build
```

Это создаст папку `/out` со статическими файлами.

### Шаг 2: Загрузите на сервер

**Вариант A: Через FTP/SFTP**

Загрузите содержимое папки `/out` в корень вашего сайта.

**Вариант B: Через SSH**

```bash
# На локальном компьютере
scp -r out/* username@your-server.com:/var/www/landing/

# Или используйте rsync
rsync -avz --delete out/ username@your-server.com:/var/www/landing/
```

### Шаг 3: Настройте Nginx

Создайте конфиг для Nginx:

```nginx
server {
    listen 80;
    server_name your-domain.com;
    
    root /var/www/landing;
    index index.html;
    
    location / {
        try_files $uri $uri.html $uri/ =404;
    }
    
    # Кэширование статики
    location ~* \.(jpg|jpeg|png|gif|svg|ico|css|js|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
    
    # GZIP
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;
    gzip_min_length 1000;
}
```

Перезагрузите Nginx:

```bash
sudo nginx -t
sudo systemctl reload nginx
```

---

## 🔄 Обновление сайта

После внесения изменений:

```bash
# 1. Соберите проект
npm run build

# 2. Загрузите на сервер
rsync -avz --delete out/ username@server:/var/www/landing/

# 3. Очистите кэш браузера
# Ctrl + Shift + R (Windows)
# Cmd + Shift + R (Mac)
```

---

## ⚙️ Автоматизация деплоя

Создайте скрипт `deploy-nextjs.sh`:

```bash
#!/bin/bash

echo "🚀 Начинаем деплой Next.js..."

# Сборка
echo "📦 Собираем проект..."
npm run build

# Загрузка на сервер
echo "📤 Загружаем на сервер..."
rsync -avz --delete out/ username@your-server.com:/var/www/landing/

# Очистка кэша (если используется Nginx)
echo "🧹 Очищаем кэш..."
ssh username@your-server.com "sudo systemctl reload nginx"

echo "✅ Деплой завершён!"
```

Использование:

```bash
chmod +x deploy-nextjs.sh
./deploy-nextjs.sh
```

---

## 📝 TODO

- [ ] Добавить данные для курсов: Химия, Биология, Математика
- [ ] Заменить `placeholder.svg` на реальные фото спикеров
- [ ] Настроить форму обратной связи (API Route или внешний сервис)
- [ ] Добавить аналитику (Google Analytics / Яндекс.Метрика)
- [ ] Заменить ссылку на Telegram канал

---

## 🆘 Помощь

### Ошибка при сборке

Удалите `node_modules` и `.next`:

```bash
rm -rf node_modules .next out
npm install
npm run build
```

### Страницы не открываются на сервере

Проверьте настройки Nginx:
- Должен быть `try_files $uri $uri.html $uri/ =404;`
- Убедитесь что `/out` полностью скопирована на сервер

---

**Версия:** 1.0.0 (Next.js)  
**Дата:** 6 февраля 2026
